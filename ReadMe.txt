1) Add the audio samples into the working directory and also keep the samples in test files.
2) Keep all function-codes and main-codes in working directory.
3) Finally after running the whole test database we generate an excel sheet in which the 3rd column contains a zero(0) which represents male and one(1) represents female.